{% extends 'base.html.twig' %}

{% block title %}SourceLevel{% endblock %}

{% block body %}
    <h1>SourceLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ source_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ source_level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_source_level_index') }}">back to list</a>

    <a href="{{ path('app_source_level_edit', {'id': source_level.id}) }}">edit</a>

    {{ include('source_level/_delete_form.html.twig') }}
{% endblock %}
